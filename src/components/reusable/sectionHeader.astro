---
const { textCenter = true, heading, subHeading } = Astro.props;
---

<div class={textCenter ? "text-center" : "text-start"}>
    <p class="play-bold text-2xl text-primary">{subHeading}</p>
    <h3 class="play-bold text-lightenDark text-5xl 2xl:text-7xl my-5">
        {heading}
    </h3>
</div>
